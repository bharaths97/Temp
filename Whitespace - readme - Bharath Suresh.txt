For Senders:
execute the python file using the following command:
<python.exe> Whitespace.py "file with message to encode" "destination file with whitespace"

For Receivers:
execute the python file using the following command:
<python.exe> Whitespace.py "file with message in whitespace"